[build-system]
# Minimum requirements for the build system to execute, according to PEP518
# specification.
requires = ["numpy", "cython<3.0", "setuptools", "wheel"]

[tool.codespell]
skip = '.git,*.pdf,*.svg,*.css,*.min.*,*.gii'
check-hidden = true
# ignore-regex = ''
# ignore-words-list = ''
